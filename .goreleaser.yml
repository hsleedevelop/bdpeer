version: 2

project_name: bdpeer

before:
  hooks:
    - go mod tidy

builds:
  # darwin: CGO required for CoreBluetooth BLE
  - id: bdpeer-darwin
    main: ./cmd/bdpeer
    binary: bdpeer
    env:
      - CGO_ENABLED=1
    goos:
      - darwin
    goarch:
      - arm64
      - amd64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

  # linux + windows: tinygo BLE scan via pure Go (no CGO needed)
  - id: bdpeer-other
    main: ./cmd/bdpeer
    binary: bdpeer
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  - ids:
      - bdpeer-darwin
      - bdpeer-other
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: hsleedevelop
    name: bdpeer
  draft: false
  prerelease: auto
